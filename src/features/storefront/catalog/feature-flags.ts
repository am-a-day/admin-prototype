/**
 * Temporary switch for the catalog-wide tag and sticker directory.
 * Keep this out of the UI: changing the constant is the only supported toggle.
 */
export const USE_SHARED_TAGS_AND_STICKERS = false;
